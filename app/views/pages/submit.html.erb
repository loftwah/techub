<section class="mx-auto max-w-6xl space-y-6 px-4 py-12">
  <h1 class="text-3xl font-bold text-slate-900 dark:text-slate-100">Submit a <span class="bg-gradient-to-r from-blue-600 via-cyan-600 to-emerald-600 bg-clip-text text-transparent dark:from-blue-400 dark:via-cyan-400 dark:to-emerald-400 animate-gradient">Profile</span></h1>
  <p class="text-slate-600 dark:text-slate-300">
    TecHub cards are free. We run an eligibility check on every GitHub account (age, repos, activity,
    followers) so the directory stays high-signal and the AI budget goes to genuine builders.
  </p>
  <ul class="list-disc space-y-2 pl-6 text-sm text-slate-500 dark:text-slate-400">
    <li>Authenticate with GitHub to attribute submissions to real builders.</li>
    <li>Provide the username you want to card and optionally add flavour text.</li>
    <li>Optionally include a personal URL we can scrape for extra context, and up to 4 GitHub repositories (owner/name) to ensure we consider them.</li>
    <li>Pass the eligibility score. If you miss, we’ll tell you what to level up and try again.</li>
    <li>We run ingestion, synthesise the card, and notify you by email.</li>
  </ul>

  <div class="rounded-lg border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
    <h2 class="mb-2 text-lg font-semibold text-slate-900 dark:text-slate-100">Submit form</h2>
    <% if current_user %>
      <form action="<%= create_submission_path %>" method="post" class="space-y-4" data-controller="submit-form">
        <%= csrf_meta_tags %>
        <div>
          <label class="block text-sm font-medium">GitHub username</label>
          <input name="login" required class="mt-1 w-full rounded border border-slate-300 bg-white p-2 text-slate-900 dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100" placeholder="octocat" />
        </div>
        <div>
          <label class="block text-sm font-medium">Personal URL to scrape (optional)</label>
          <input name="submitted_scrape_url" class="mt-1 w-full rounded border border-slate-300 bg-white p-2 text-slate-900 dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100" placeholder="https://example.com/me" />
        </div>
        <div>
          <label class="block text-sm font-medium">GitHub repositories (up to 4)</label>
          <div class="grid gap-2 md:grid-cols-2">
            <% 4.times do |i| %>
              <input name="submitted_repositories[]" class="mt-1 w-full rounded border border-slate-300 bg-white p-2 text-slate-900 dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100" placeholder="owner/repo" />
            <% end %>
          </div>
        </div>
        <button type="submit" 
                data-submit-form-target="button"
                class="inline-flex items-center justify-center gap-2 cursor-pointer rounded bg-slate-900 px-6 py-2.5 font-medium text-white transition-all duration-200 hover:bg-slate-700 hover:shadow-lg hover:scale-105 active:scale-95 focus:outline-none focus:ring-2 focus:ring-slate-500 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:scale-100 dark:bg-slate-100 dark:text-slate-900 dark:hover:bg-slate-200 dark:focus:ring-slate-400">
          <span data-submit-form-target="text">Submit Profile</span>
          <svg data-submit-form-target="spinner" class="hidden animate-spin h-5 w-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
          </svg>
        </button>
      </form>
    <% else %>
      <form action="<%= login_path %>" method="get" data-turbo-frame="_top" class="flex flex-col sm:flex-row gap-2 items-stretch">
        <input type="text" name="invite_code" placeholder="Invite code (optional)" class="rounded border border-slate-300 bg-white px-4 py-2 text-slate-900 dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100" />
        <button type="submit" class="inline-block rounded px-4 py-2 text-white bg-blue-600 hover:bg-blue-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600 dark:bg-blue-600 dark:hover:bg-blue-500">Sign in with GitHub</button>
      </form>
    <% end %>
  </div>
</section>
